export type TUser = {
  id: string;
  name: string;
  email: string;
  avatar: string;
  number: number;
};

export type TUsers = {
  id: string;
  name: string;
  email: string;
  avatar: string;
  role: string;
}[];
